# Curso de LangGraph — dependencias verificadas contra LangGraph 1.2 / LangChain 1.3
# Instalación:  pip install -r requirements.txt

# --- núcleo ---------------------------------------------------------------
langgraph>=1.2,<2
langchain>=1.3,<2
langchain-core>=1.6,<2
langchain-openai>=1.6,<2

# --- persistencia ---------------------------------------------------------
langgraph-checkpoint>=4.2          # interfaz base + InMemorySaver
langgraph-checkpoint-sqlite>=3.1   # SqliteSaver (módulo 3)
# langgraph-checkpoint-postgres>=3.1  # descomenta si vas a probar Postgres

# --- RAG (módulo 5) -------------------------------------------------------
langchain-text-splitters>=1.0
rank-bm25>=0.2                     # recuperación léxica para el híbrido del módulo 5
# Nota: NO usamos langchain-community. Está en proceso de retirada y sus integraciones
# se están moviendo a paquetes propios; el curso implementa el recuperador léxico
# directamente sobre rank-bm25, que además se entiende mejor.

# --- utilidades de notebook ----------------------------------------------
jupyterlab>=4.2
ipywidgets>=8.1
python-dotenv>=1.0
pandas>=2.2
matplotlib>=3.9
grandalf>=0.8                      # dibujo ASCII de grafos sin salir a internet

# --- evaluación y pruebas (módulo 6) -------------------------------------
langsmith>=0.11
pytest>=8.2
pytest-asyncio>=0.24

# --- despliegue (módulo 6, opcional) -------------------------------------
# langgraph-cli[inmem]>=0.4        # `langgraph dev` para LangGraph Studio en local
