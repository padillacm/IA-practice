.env
.ipynb_checkpoints/
__pycache__/
*.pyc
.pytest_cache/

# Caché de peticiones al modelo del plugin de pytest (notebook 10). Esta SÍ suele
# versionarse en un proyecto real —es lo que hace las pruebas deterministas y gratis—
# pero aquí no, porque se genera con tu clave y contiene tus respuestas.
pruebas/cassettes/

# Entornos y cachés de uv
.venv/
.uv/
# uv.lock SÍ se versiona: es lo que hace reproducible el curso.

# Grabaciones que genera el notebook 10 mientras se ejecuta. Se borran solas al final,
# pero si se interrumpe a mitad no queremos que acaben en el repositorio.
langsmith/_cache_del_notebook/
_cache_del_notebook/
